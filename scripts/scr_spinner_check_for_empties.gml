/*for(var i = 0; i < numberOfColumns; i++)
{
    for(var j = 0; j < symbolsPerColumn; j++)
    {
        if(symbolObject[i, j] == noone)
        {
            scr_spinner_shift(noone,i,j, 0)
        }
    }
}
//scr_spinner_reset();
