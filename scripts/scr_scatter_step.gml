/*if (obj_slot_machine.slotState == 0 && global.scatterActive == 2)
{
    with(<scatter object>)
    {
        if (place_meeting(x,y, obj_spinner_window))
        {
            scatterCount ++;
        }
    }
    
    switch(scatterCount)
    {
        case 0: // do something
                break;
        case 1: // do something
                break;
        case 2: // do something
                break;
        case 3: // do something
                break;
    }
    global.scatterActive = 1;
}*/ //has beenadded as a piece of code instead of script
