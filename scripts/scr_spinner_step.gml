switch(slotState)
{
    case (0): // not spinning
        scr_spinner_flick()
        break;
    case (1): // spinning
        scr_spinner_spin();
        break;
    //case (2): // tallying the score
}
